/*Lab 13
WAP that determines if the subsequence of number is part of an array.*/
#include<stdio.h>
#include<conio.h>
main()
{
	int i,j,k,n=10,a[i]={3,5,};
	for(i=0;i<n;i++)
	{
		if(a[i]==b[0])
		{
			k=i+1;
			for(j=1;)
			{
				if(b[j]!=a[k])
				{
					flag=1;
					break;
				}
			}
		}
	}
	getch();
}
